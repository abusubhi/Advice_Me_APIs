@model List<Advice_Me_APIs.DTOs.ProductDTO>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Product Management</h2>

<table class="table">
    <thead>
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Category</th>
            <th>Status</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var product in Model)
        {
            <tr>
                <td>@product.Id</td>
                <td>@product.Name</td>
                <td>@product.CategoryID</td>
                <td>@product.ModelNumber</td>
                <td>
                    @if (product.ModelNumber != "Approved")
                    {
                        <a asp-action="Approve" asp-route-id="@product.Id">Approve</a>
                    }
                    <a asp-action="Delete" asp-route-id="@product.Id" style="color:red;">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
